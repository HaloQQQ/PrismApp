<UserControl
    x:Class="CustomControlsDemoModule.Views.ChineseChessView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:chinesechess="clr-namespace:CustomControlsDemoModule.Models.Games.ChineseChess"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    FocusVisualStyle="{x:Null}"
    MyAttachProperty.IsElementFocused="True">

    <prism:Dialog.WindowStyle>
        <Style BasedOn="{StaticResource Window_WithoutTitleBar}" TargetType="Window">
            <Setter Property="MinWidth" Value="1200" />
            <Setter Property="MinHeight" Value="640" />
        </Style>
    </prism:Dialog.WindowStyle>

    <UserControl.Resources>
        <ResourceDictionary Source="/CustomControlsDemoModule;component/Resources/Games/ChineseChess.xaml" />
    </UserControl.Resources>

    <UserControl.InputBindings>
        <KeyBinding
            Key="{Binding KeyGestureDic[重玩].SelectedKey}"
            Command="{Binding RePlayCommand}"
            Modifiers="{Binding KeyGestureDic[重玩].ModifierKey}" />

        <KeyBinding
            Key="{Binding KeyGestureDic[播放/暂停].SelectedKey}"
            Command="{Binding StartPauseCommand}"
            Modifiers="{Binding KeyGestureDic[播放/暂停].ModifierKey}" />

        <KeyBinding
            Key="{Binding KeyGestureDic[悔棋].SelectedKey}"
            Command="{Binding CancelLastCommand}"
            Modifiers="{Binding KeyGestureDic[悔棋].ModifierKey}" />

        <KeyBinding
            Key="{Binding KeyGestureDic[棋盘换向].SelectedKey}"
            Command="{Binding SwitchDirectionCommand}"
            Modifiers="{Binding KeyGestureDic[棋盘换向].ModifierKey}" />
    </UserControl.InputBindings>

    <Grid>
        <DockPanel>
            <WindowTitleBar DockPanel.Dock="Top" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="4*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Margin="5,0,5,10"
                    Background="LightGray"
                    CornerRadius="10" />

                <Viewbox Grid.Column="1" DockPanel.Dock="Bottom">
                    <StackPanel>
                        <DockPanel Height="80" Background="Red">
                            <StackPanel
                                Background="#2874A6"
                                DockPanel.Dock="Left"
                                Orientation="Horizontal">

                                <Image Margin="5" Source="../../Resources/Images/默认头像.png" />

                                <Ellipse
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    HorizontalAlignment="Left">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="../../Resources/Pictures/黑将.png" />
                                    </Ellipse.Fill>
                                    <Ellipse.Style>
                                        <Style BasedOn="{StaticResource ChineseChess_Signal}" TargetType="Ellipse">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRedTurn}" Value="True">
                                                    <Setter Property="Stroke" Value="Transparent" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>

                                <TextBlock
                                    Margin="3"
                                    FontSize="20"
                                    Style="{StaticResource TextBlock_Theme}"
                                    Text="{Binding BlackTimeSpan, FallbackValue=00:00:00}" />
                            </StackPanel>

                            <StackPanel
                                Background="#DC7633"
                                DockPanel.Dock="Right"
                                Orientation="Horizontal">
                                <TextBlock
                                    Margin="3"
                                    FontSize="20"
                                    Style="{StaticResource TextBlock_Theme}"
                                    Text="{Binding RedTimeSpan, FallbackValue=00:00:00}" />

                                <Ellipse Grid.Row="1" Grid.Column="0">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="../../Resources/Pictures/红帅.png" />
                                    </Ellipse.Fill>
                                    <Ellipse.Style>
                                        <Style BasedOn="{StaticResource ChineseChess_Signal}" TargetType="Ellipse">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRedTurn}" Value="False">
                                                    <Setter Property="Stroke" Value="Transparent" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>

                                <Image Margin="5" Source="../../Resources/Images/默认头像.png" />
                            </StackPanel>

                            <WrapPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                DockPanel.Dock="Left">
                                <Button Style="{StaticResource StartPauseButton}" />
                                <Button Style="{StaticResource RePlayButton}" />

                                <Button Style="{StaticResource CancelLastButton}" />

                                <Button Style="{StaticResource SwitchDirectionButton}" />
                            </WrapPanel>
                        </DockPanel>

                        <Grid Margin="0,5,0,10">
                            <Image>
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Source" Value="../../Resources/Pictures/棋盘（黑上）.png" />

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Angle}" Value="180">
                                                <Setter Property="Source" Value="../../Resources/Pictures/棋盘（红上）.png" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>

                            <ListBox
                                Padding="11"
                                ItemContainerStyle="{StaticResource ListBoxItem_General}"
                                ItemTemplate="{StaticResource Chess}"
                                ItemsSource="{Binding Datas}"
                                RenderTransformOrigin="0.5,0.5">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="9" Rows="10" />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.RenderTransform>
                                    <RotateTransform Angle="{Binding DataContext.Angle, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                                </ListBox.RenderTransform>
                            </ListBox>

                            <Grid Background="#AA999999" Visibility="{Binding IsUsable, Converter={StaticResource BoolToCollapsedVisibleConverter}}">
                                <Button
                                    FontSize="60"
                                    Foreground="Gray"
                                    Style="{StaticResource StartPauseButton}"
                                    Visibility="{Binding IsGameOver, Converter={StaticResource BoolToCollapsedVisibleConverter}}" />

                                <Button
                                    FontSize="60"
                                    Foreground="Gray"
                                    Style="{StaticResource RePlayButton}"
                                    Visibility="{Binding IsGameOver, Converter={StaticResource BoolToVisibleCollapsedConverter}}" />
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Viewbox>

                <Border
                    Grid.Column="2"
                    Margin="5,0,5,10"
                    Background="LightGray"
                    CornerRadius="10">
                    <ListBox
                        VerticalAlignment="Bottom"
                        ItemContainerStyle="{x:Null}"
                        ItemsSource="{Binding Stack}">
                        <ListBox.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="10,5" />
                                <Setter Property="Foreground" Value="Black" />

                                <Setter Property="FontSize" Value="30" />

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRed}" Value="True">
                                        <Setter Property="Foreground" Value="Red" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.Resources>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type chinesechess:ChinChessCommand}">
                                <TextBlock Style="{StaticResource {x:Type TextBlock}}">
                                    <TextBlock>
                                        [
                                        <TextBlock Text="{Binding Index}" />
                                        ]
                                        (
                                        <TextBlock Text="{Binding FromRow}" />
                                        ,
                                        <TextBlock Text="{Binding FromColumn}" />
                                        ) => (
                                        <TextBlock Text="{Binding ToRow}" />
                                        ,
                                        <TextBlock Text="{Binding ToColumn}" />
                                        )
                                    </TextBlock>
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
