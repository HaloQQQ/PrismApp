<UserControl
    x:Class="MusicPlayerModule.Views.LyricView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:converters="clr-namespace:MusicPlayerModule.Converters"
    xmlns:utils="clr-namespace:MusicPlayerModule.Utils"
    xmlns:viewmodels="clr-namespace:MusicPlayerModule.ViewModels"
    Background="{DynamicResource BackPanel.Background}">
    <UserControl.RenderTransform>
        <TranslateTransform Y="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </UserControl.RenderTransform>

    <UserControl.Resources>
        <ResourceDictionary Source="/MusicPlayerModule;component/Resources/Music/Lyric.xaml" />
    </UserControl.Resources>

    <Border CommandManager.Executed="UserControl_Executed">
        <Border.InputBindings>
            <MouseBinding Command="{x:Static MediaCommands.TogglePlayPause}" MouseAction="LeftClick" />
        </Border.InputBindings>
        <DockPanel>
            <DockPanel
                Margin="20,5"
                HorizontalAlignment="Stretch"
                DockPanel.Dock="Top">
                <Button
                    Command="{x:Static NavigationCommands.GoToPage}"
                    Content="&#xe65e;"
                    DockPanel.Dock="Left"
                    Style="{StaticResource ButtonBase_Icon_Small_HoverBlue}" />

                <ToggleButton
                    x:Name="LyricSettingButton"
                    Content="&#xe8b8;"
                    DockPanel.Dock="Right"
                    FontSize="20"
                    Style="{StaticResource ButtonBase_Icon_ScaleDown_Big}" />
                <Popup
                    IsOpen="{Binding IsChecked, ElementName=LyricSettingButton}"
                    MouseLeftButtonDown="EmptyHandler"
                    Placement="Left"
                    PlacementTarget="{Binding ElementName=LyricSettingButton}">
                    <Border
                        Width="400"
                        Padding="20"
                        Background="{StaticResource PopupBackground}"
                        CornerRadius="10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    FontSize="20"
                                    Style="{StaticResource TextBlock_Theme}"
                                    Text="大小：" />
                                <StackPanel VerticalAlignment="Center">
                                    <Slider
                                        x:Name="LyricFontSizeSlider"
                                        Width="287"
                                        Height="3"
                                        Margin="5"
                                        HorizontalAlignment="Left"
                                        Maximum="40"
                                        Minimum="15"
                                        Style="{StaticResource Slider_ObviousThumb}"
                                        TickFrequency="5"
                                        Value="{Binding DesktopLyric.CurrentLyricFontSize}" />

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            Margin="-2,0,0,0"
                                            Style="{StaticResource TextBlock_Theme}"
                                            Text="15" />
                                        <TextBlock
                                            Margin="37,0,0,0"
                                            Style="{StaticResource TextBlock_Theme}"
                                            Text="20" />
                                        <TextBlock
                                            Margin="38,0,0,0"
                                            Style="{StaticResource TextBlock_Theme}"
                                            Text="25" />
                                        <TextBlock
                                            Margin="38,0,0,0"
                                            Style="{StaticResource TextBlock_Theme}"
                                            Text="30" />
                                        <TextBlock
                                            Margin="38,0,0,0"
                                            Style="{StaticResource TextBlock_Theme}"
                                            Text="35" />
                                        <TextBlock
                                            Margin="37,0,0,0"
                                            Style="{StaticResource TextBlock_Theme}"
                                            Text="40" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    FontSize="20"
                                    Style="{StaticResource TextBlock_Theme}"
                                    Text="颜色：" />

                                <StackPanel>
                                    <ListBox
                                        x:Name="LyricForegroundListBox"
                                        Margin="40,5"
                                        ItemsSource="{Binding DesktopLyric.DefaultLyricForegrounds}">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel MaxWidth="220" />
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                        <ListBox.ItemContainerStyle>
                                            <Style BasedOn="{StaticResource ListBoxItem_General}" TargetType="ListBoxItem">
                                                <Setter Property="BorderBrush" Value="Transparent" />
                                                <Setter Property="Background" Value="{Binding}" />
                                                <Setter Property="BorderThickness" Value="2" />
                                                <Setter Property="ToolTip" Value="{Binding}" />

                                                <EventSetter Event="MouseMove" Handler="DragLeaveFrom" />

                                                <Setter Property="Width" Value="40" />
                                                <Setter Property="Height" Value="40" />

                                                <Setter Property="MyAttachProperty.CornerRadius" Value="20" />
                                                <Setter Property="Content" Value="{x:Null}" />

                                                <Style.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource HoveredTransparentButtonBackground}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>

                                    <StackPanel Orientation="Horizontal">
                                        <Border
                                            Width="40"
                                            Height="40"
                                            Margin="10,0"
                                            Background="{Binding DesktopLyric.CustomColor}"
                                            CornerRadius="20"
                                            MouseMove="DragLeaveFrom"
                                            ToolTip="{Binding DesktopLyric.CustomColor}" />

                                        <ColorPicker
                                            Width="200"
                                            Height="200"
                                            Background="LightBlue"
                                            SelectedBrush="{Binding DesktopLyric.CustomColor, Mode=TwoWay}" />
                                    </StackPanel>

                                    <StackPanel Margin="20,10,0,0" Orientation="Horizontal">
                                        <ListBox
                                            ItemContainerStyle="{StaticResource ListBoxItem_General}"
                                            ItemTemplate="{StaticResource ThreePrimaryColorModel}"
                                            ItemsSource="{Binding DesktopLyric.LyricColorBrush.Colors}" />

                                        <Border
                                            Width="40"
                                            Height="40"
                                            AllowDrop="True"
                                            Background="{Binding DesktopLyric.LyricColorBrush.ColorBrush, Mode=TwoWay}"
                                            CornerRadius="20"
                                            Drop="DropIn_Color"
                                            ToolTip="{Binding DesktopLyric.LyricColorBrush.ColorBrush.Color}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Margin="0,10" Orientation="Horizontal">
                                <TextBlock
                                    FontSize="20"
                                    Style="{StaticResource TextBlock_Theme}"
                                    Text="渐变：" />

                                <ListBox
                                    Margin="20,0,0,0"
                                    ItemContainerStyle="{StaticResource ListBoxItem_General}"
                                    ItemTemplate="{StaticResource ThreePrimaryColorModel}"
                                    ItemsSource="{Binding DesktopLyric.LinearGradientColorBrush.Colors}" />

                                <Border
                                    Width="40"
                                    Height="40"
                                    AllowDrop="True"
                                    Background="{Binding DesktopLyric.LinearGradientColorBrush.ColorBrush, Mode=TwoWay}"
                                    CornerRadius="20"
                                    Drop="DropIn_Color"
                                    ToolTip="{Binding DesktopLyric.LinearGradientColorBrush.ColorBrush.Color}" />

                                <Button
                                    MinWidth="0"
                                    Padding="10"
                                    Command="{Binding DesktopLyric.LinearGradientColorBrush.ResetColorCommand}"
                                    Content="重置"
                                    FontSize="14"
                                    MyAttachProperty.CornerRadius="10"
                                    Style="{StaticResource ButtonBase_Icon_Conent_Transparent}" />
                            </StackPanel>

                            <WrapPanel>
                                <TextBlock
                                    FontSize="20"
                                    Style="{StaticResource TextBlock_Theme}"
                                    Text="字体：" />
                                <ListBox
                                    ItemsSource="{Binding DesktopLyric.Fonts}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    SelectedItem="{Binding DesktopLyric.CurrentFontModel}">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemContainerStyle>
                                        <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="ListBoxItem">
                                            <Setter Property="MyAttachProperty.CornerRadius" Value="10" />
                                            <Style.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource TransparentButtonBackground}" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate DataType="viewmodels:FontModel">
                                            <TextBlock
                                                MinWidth="60"
                                                Padding="10,5"
                                                FontFamily="{Binding Family}"
                                                FontSize="14"
                                                Text="{Binding DisplayName}"
                                                TextAlignment="Center"
                                                TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Popup>
            </DockPanel>

            <DockPanel Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource DigitSubConverter}, ConverterParameter=140}" VerticalAlignment="Top">
                <TextBlock
                    HorizontalAlignment="Center"
                    DockPanel.Dock="Top"
                    FontFamily="{Binding DesktopLyric.CurrentFontModel.Family}"
                    FontSize="21"
                    FontWeight="Bold"
                    Foreground="{Binding DesktopLyric.LyricColorBrush.ColorBrush, FallbackValue=SkyBlue}"
                    Text="{Binding CurrentMedia.Music.Name}" />

                <TextBlock
                    Margin="0,10"
                    HorizontalAlignment="Center"
                    DockPanel.Dock="Top"
                    FontFamily="{Binding DesktopLyric.CurrentFontModel.Family}"
                    Foreground="{Binding DesktopLyric.LyricColorBrush.ColorBrush, FallbackValue=SkyBlue}"
                    Text="{Binding CurrentMedia.Music.Singer}" />

                <StackPanel
                    x:Name="LyricSimple"
                    Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=DockPanel}}"
                    Margin="0,30,0,0"
                    VerticalAlignment="Bottom"
                    Visibility="Collapsed">
                    <ContentControl
                        HorizontalAlignment="Left"
                        Content="{Binding CurrentMedia.OneLine}"
                        ContentTemplate="{StaticResource PanelSimpleLyric}"
                        FontFamily="{Binding DesktopLyric.CurrentFontModel.Family}"
                        FontSize="30" />

                    <ContentControl
                        HorizontalAlignment="Right"
                        Content="{Binding CurrentMedia.AnotherLine}"
                        ContentTemplate="{StaticResource PanelSimpleLyric}"
                        FontFamily="{Binding DesktopLyric.CurrentFontModel.Family}"
                        FontSize="30" />
                </StackPanel>

                <Grid>
                    <Viewbox
                        Width="300"
                        Height="300"
                        HorizontalAlignment="Left">
                        <Image>
                            <Image.Clip>
                                <EllipseGeometry
                                    Center="30,30"
                                    RadiusX="30"
                                    RadiusY="30" />
                            </Image.Clip>
                            <Image.RenderTransform>
                                <RotateTransform />
                            </Image.RenderTransform>
                            <Image.Style>
                                <Style BasedOn="{StaticResource MusicImage}" TargetType="Image">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard x:Name="Rotate_MusicCover">
                                                <Storyboard>
                                                    <DoubleAnimation
                                                        RepeatBehavior="Forever"
                                                        Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                                        From="0"
                                                        To="360"
                                                        Duration="0:0:30" />
                                                </Storyboard>
                                            </BeginStoryboard>

                                            <PauseStoryboard BeginStoryboardName="Rotate_MusicCover" />
                                        </EventTrigger>
                                        <DataTrigger Binding="{Binding Running}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <ResumeStoryboard BeginStoryboardName="Rotate_MusicCover" />
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <PauseStoryboard BeginStoryboardName="Rotate_MusicCover" />
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Running}" Value="True" />
                                                <Condition Binding="{Binding CurrentMedia.CurrentMills}" Value="0" />
                                            </MultiDataTrigger.Conditions>
                                            <MultiDataTrigger.EnterActions>
                                                <SeekStoryboard BeginStoryboardName="Rotate_MusicCover" />
                                            </MultiDataTrigger.EnterActions>
                                        </MultiDataTrigger>
                                        <DataTrigger Binding="{Binding CurrentMedia.Music.ImageSource}" Value="{x:Null}">
                                            <Setter Property="Source">
                                                <Setter.Value>
                                                    <BitmapImage UriSource="/MusicPlayerModule;component/Resources/音符.png" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Viewbox>

                    <Grid>
                        <ListBox
                            x:Name="LyricList"
                            Focusable="False"
                            ItemsSource="{Binding CurrentMedia.Music.Lyric.Lines}"
                            OpacityMask="{StaticResource GradientOpacityMask}"
                            ScrollViewer.VerticalScrollBarVisibility="Disabled"
                            SelectionChanged="LyricList_SelectionChanged">
                            <ListBox.ItemContainerStyle>
                                <Style BasedOn="{StaticResource LyricListBoxItem}" TargetType="ListBoxItem">
                                    <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick" />
                                    <EventSetter Event="PreviewMouseRightButtonDown" Handler="SwitchLyricMode" />

                                    <Setter Property="Padding" Value="0,10" />

                                    <Setter Property="Width" Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=DockPanel}}" />

                                    <Setter Property="IsSelected" Value="{Binding IsPlayingLine, Mode=OneWay}" />

                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPlayingLine}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            DecelerationRatio="0.5"
                                                            Storyboard.TargetProperty="(ListBoxItem.RenderTransform).(ScaleTransform.ScaleX)"
                                                            To="1.5"
                                                            Duration="0:0:0.5" />
                                                        <DoubleAnimation
                                                            DecelerationRatio="0.5"
                                                            Storyboard.TargetProperty="(ListBoxItem.RenderTransform).(ScaleTransform.ScaleY)"
                                                            To="1.5"
                                                            Duration="0:0:0.5" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            DecelerationRatio="0.5"
                                                            Storyboard.TargetProperty="(ListBoxItem.RenderTransform).(ScaleTransform.ScaleX)"
                                                            To="1.0"
                                                            Duration="0:0:0.5" />
                                                        <DoubleAnimation
                                                            DecelerationRatio="0.5"
                                                            Storyboard.TargetProperty="(ListBoxItem.RenderTransform).(ScaleTransform.ScaleY)"
                                                            To="1.0"
                                                            Duration="0:0:0.5" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>

                        <NeedleWaitLoad
                            Width="80"
                            Height="80"
                            Foreground="DodgerBlue"
                            Visibility="{Binding CurrentMedia.Music.IsLoadingLyric, Converter={StaticResource BoolToVisibleCollapsedConverter}, FallbackValue={x:Static Visibility.Collapsed}}" />
                    </Grid>
                </Grid>
            </DockPanel>
        </DockPanel>

    </Border>
</UserControl>
